<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jenkins Practice for DevOps - 2004</title>
    <style>
        /* Basic Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(to bottom, #f4f4f9, #e0e4e9);
            color: #333;
            line-height: 1.6;
            padding: 20px;
        }

        h1 {
            background-color: #4CAF50;
            color: white;
            padding: 20px;
            text-align: center;
            margin-bottom: 20px;
            border-radius: 5px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        h2 {
            font-size: 1.5em;
            color: #4CAF50;
            text-align: center;
            margin-top: 30px;
            margin-bottom: 15px;
        }

        h3 {
            font-size: 1.2em;
            color: #666;
            text-align: center;
            margin-top: 10px;
            margin-bottom: 30px;
        }

        /* Todo List Styling */
        ul {
            list-style: none;
            padding: 0;
            max-width: 600px;
            margin: 0 auto;
        }

        ul li {
            background: #fff;
            border: 1px solid #ddd;
            margin-bottom: 12px;
            padding: 15px;
            position: relative;
            border-radius: 5px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        ul li:hover {
            transform: scale(1.02);
        }

        /* Links Styling */
        ul li a {
            color: #333;
            text-decoration: none;
            font-weight: bold;
            padding: 5px 10px;
            border-radius: 3px;
        }

        ul li a:hover {
            opacity: 0.7;
        }

        /* Delete Button */
        .delete-btn {
            color: red;
            position: absolute;
            right: 10px;
            top: 10px;
        }

        /* Edit Button */
        .edit-btn {
            color: #FFC107;
            position: absolute;
            right: 40px;
            top: 10px;
        }

        /* Form Styling */
        form {
            background: #fff;
            padding: 20px;
            margin-top: 30px;
            border-radius: 5px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: 0 auto;
        }

        label {
            display: block;
            margin-bottom: 10px;
            font-weight: bold;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
        }

        input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }

        input[type="submit"]:hover {
            background-color: #45a049;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            h1 {
                font-size: 1.5em;
            }

            h2 {
                font-size: 1.2em;
            }

            h3 {
                font-size: 1em;
            }

            form {
                padding: 15px;
            }

            ul li {
                padding: 12px;
            }

            input[type="text"], input[type="submit"] {
                font-size: 14px;
            }
        }
    </style>
</head>

<body>
    <h1>Todo List - Practice CI/CD - Amitabh DevOps 2004</h1>
    <h2>Welcome to DevOps Batch - 08</h2>
    <h3>Mastering Jenkins, Git, and CI/CD</h3>
    <h3>Join the TrainWithShubham Community</h3>

    <h2>Your Tasks:</h2>
    <ul>
        <% todolist.forEach(function(todo, index) { %>
        <li>
            <a href="/todo/delete/<%= index %>" class="delete-btn">✘</a>
            <a href="/todo/<%= index %>" class="edit-btn">✎</a>
            <%- todo %>
        </li>
        <% }); %>
    </ul>

    <h2>Add New Task</h2>
    <form action="/todo/add/" method="post">
        <label for="newtodo">What should I do?</label>
        <input type="text" name="newtodo" id="newtodo" placeholder="Enter your task..." autofocus required />
        <input type="submit" value="Add Task" />
    </form>

    <footer style="text-align: center; margin-top: 50px; color: #777;">
        <p>&copy; 2024 Amitabh. All rights reserved.</p>
    </footer>
</body>
</html>
